# AI Health Buddy (Strict RAG Version) 🏥🤖

โปรเจกต์แชทบอทเพื่อสุขภาพที่ให้คำแนะนำเบื้องต้น  
โดยใช้เทคโนโลยี RAG (Retrieval-Augmented Generation)  
เพื่อให้ AI ตอบจากฐานข้อมูลที่กำหนดเท่านั้น ลดปัญหาการมั่วข้อมูล

> ⚠️ ระบบนี้ให้คำแนะนำเบื้องต้น ไม่ใช่การวินิจฉัยทางการแพทย์

---

## 🌟 ฟีเจอร์หลัก

- **Symptom Check:** รับอาการและสอบถามเพิ่มเติมแบบธรรมชาติ คล้ายเภสัชกร
- **Medication Guidance:** แนะนำยาเบื้องต้นเฉพาะเมื่อมีข้อมูลในฐานข้อมูล
- **Strict RAG Safety:** ตอบจากเอกสารอ้างอิงในระบบเท่านั้น (ถ้าไม่มีข้อมูลจะตอบว่าไม่เพียงพอ)
- **Secure API Config:** ใช้ `.env` และ Streamlit Secrets เพื่อความปลอดภัยของ API Key

---

## 🛠 Tech Stack

- **Language:** Python 3.10+
- **Framework:** Streamlit
- **LLM Engine:** Groq API (Model: llama-3.3-70b-versatile)
- **RAG Framework:** LangChain
- **Database:** ChromaDB (Vector Database)

---

## ⚙️ วิธีติดตั้ง (Installation)

### 1. เตรียมเครื่องมือ

ต้องติดตั้งโปรแกรมเหล่านี้ก่อน:

- [Python](https://www.python.org/downloads/) (ตอนลงอย่าลืมติ๊ก ☑ Add Python to PATH)

---

### 2. Clone โปรเจค

```bash
git clone https://github.com/Thanwa14/AI-Health-Buddy.git
cd AI-Health-Buddy
```

---

### 3. ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

---

### 4. ตั้งค่า API Key

สร้างไฟล์ `.env` แล้วเพิ่ม:

```
GROQ_API_KEY=your_api_key_here
```

> หมายเหตุ: ไฟล์ `.env` จะไม่ถูก push ขึ้น GitHub

---

### 5. รันแอป

```bash
streamlit run app.py
```

---

## ☁️ Deploy บน Streamlit Cloud

1. Push โค้ดขึ้น GitHub
2. ไปที่ Streamlit Cloud
3. ไปที่ Settings → Secrets
4. เพิ่ม:

```
GROQ_API_KEY = "your_api_key_here"
```

---

## 🔒 Safety Policy

- ใช้ข้อมูลจาก Context เท่านั้น
- ไม่ใช้ความรู้ภายนอกระบบ
- ไม่วินิจฉัยโรค
- หากไม่มีข้อมูลในระบบ จะตอบว่า  
  > "ขออภัย ข้อมูลในระบบไม่เพียงพอสำหรับการแนะนำ"
